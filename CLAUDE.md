# gb-dashboard – Claude instructions

## GitHub account
Always use the `Blaxzter` GitHub account for all `gh` CLI operations.
Before any `gh` command, verify the active account and switch if needed:
```bash
gh auth switch --user Blaxzter
```

## Asking questions
Never post clarifying questions as GitHub issue comments.
Ask the user directly in the chat instead.
