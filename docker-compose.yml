version: "3"

networks:
  nginx-net:
    external:
      name: nginx-net

services:
  frontend:
    build: .
    restart:
      always
    ports:
      - "8082:80"
    networks:
      - nginx-net
    environment:
      VIRTUAL_HOST: gb-dashboard.fabraham.dev, gb26.***REMOVED***
      LETSENCRYPT_HOST: gb-dashboard.fabraham.dev, gb26.***REMOVED***
      LETSENCRYPT_EMAIL: ***REMOVED***